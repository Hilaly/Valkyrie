###### This is simple example of some expression.
<!-- And this is HTML like comment -->

<root>:
    => $name
    => log name.

<entities_creation>:
    => "Entities :" <entities_list>
    => log "entities parsed", call <entities_list>.

<entities_list>:
    => <list_marker> $entityType <entities_list>
    => log "founded entity "+ entityType, call <entities_list>.
<entities_list>:
    => <list_marker> $entityType
    => log "founded entity "+ entityType.

## Entities: X.

## X is Y property.

## X contains Y.
## X contains list(collection) of Y.

## X has Y.

## Every tick X Y.
## Every T seconds A.

## A is change X to Y.
## A is change X by Y.



### Control sequences, this sequences skipped.
<list_marker>:
    => "- [ ]"
    => skip.
<list_marker>:
    => "-"
    => skip.
