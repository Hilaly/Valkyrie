<define_flag> :
    => name "is flag" 
    => create struct name.
   
<define_component> :
    => name "is" component 
    => create class name + component.

<define_class> :
    => name "is class" 
    => create class name.

<define_struct> :
    => name "is struct"
     => create struct name.